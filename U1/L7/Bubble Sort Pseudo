myList <- [1,3,2,4,5]
swapped <- True
WHILE swapped
  FOR i <- IN LEN(MyList) - 1
    IF myList[i] > myList[i]
      temp <- myList[i]
      myList[i] <- myList[i+1]
      myList[i+1] <- temp
      swapped <- True
    ENDIF
  ENDFOR
ENDWHILE
